export interface SearchResultModel {
  latitude: number;
  longitude: number;
  firstImageUrl: string;
  placeName: string;
  description: string;
  address: string;
  bookmarked: boolean;
  placeId: number;
}
